import qrcode
#change this link in url to the google form
url = "http://127.0.0.1:5500/new2.html"
img = qrcode.make(url)
# Save the QR code
img.save("form_web.png")
#after form direct user to start with a prompt make sure you have your headphones on!!!
#remove ## cause ## is the actual code.
